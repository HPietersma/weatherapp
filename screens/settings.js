import { View, Text, SafeAreaView } from "react-native";

export default function Settings() {
    return (
        <SafeAreaView>
            <Text>
                settings
            </Text>
        </SafeAreaView>
    );
}